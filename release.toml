publish = false
tag-name = "v{{version}}"
pre-release-commit-message = "release: bump to v{{version}}"
pre-release-hook = ["bash", "-c", "cd $(git rev-parse --show-toplevel) && ./scripts/release-prep.sh {{version}}"]
